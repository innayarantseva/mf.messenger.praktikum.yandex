import { Chats } from './chatlist.js';

export const page = new Chats(undefined);

// function renderToDom(query, block) {
//     const root = document.querySelector(query);
//     root.appendChild(block.getContent());
//     return root;
// }

// renderToDom('.app', page);
